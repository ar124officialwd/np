# np

> [!SUMMARY]
> `np` is NeoVim, NixVim, NextVim
> `np` is NeoPod, No a Problem
> `np` is for NeoVim Project Oriented Development
> Thanks to Nvim, NixVim, Nix, LazyVim and many all plugin authors
